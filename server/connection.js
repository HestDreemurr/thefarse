class ServerConnection {
  constructor(client, msg, rooms) {
    this.client = client
    this.msg = msg
    this.rooms = rooms
  }
  
  createRoom() {
        
  }
  
  joinRoom() {
    
  }
}

module.exports = ServerConnection